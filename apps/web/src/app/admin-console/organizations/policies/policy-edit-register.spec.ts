import { ossPolicyEditRegister } from "./policy-edit-register";

describe("ossPolicyEditRegister", () => {
  it("is not empty (sanity check)", () => {
    expect(ossPolicyEditRegister.length).toBeGreaterThan(0);
  });
});
