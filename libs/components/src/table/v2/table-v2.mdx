import { Meta, Story, Primary } from "@storybook/addon-docs/blocks";

import * as stories from "./table-v2.stories";

<Meta of={stories} />

# Table V2

`bit-table-v2` is a column-driven evolution of `bit-table`. Each column is a `<bit-column>` wrapper
containing a `<bit-header-cell>` for the header markup and a `*bitCellDef` cell template for the
per-row cell markup. The table renders a CSS-grid layout of ARIA-role elements
(`role="table"`/`"row"`/`"columnheader"`/`"cell"`); the consumer's `<bit-header-cell>`/`<bit-cell>`
become the grid items, and column widths map to grid tracks.

A table is configured by a single `TableModel`, bound via `[table]`. The model composes the focused
sub-models — data, columns, filter, and (optional) selection — so a table is built and bound once.

Use `bit-table-v2` for new tables. The legacy `bit-table` continues to work and isn't being migrated
— see [Table](?path=/docs/component-library-table--docs) for its docs.

<Primary />

## Quick start

```ts
type User = { id: number; name: string; email: string };

class UserListComponent {
  protected readonly table = new TableModel<User>({ data: this.users });
  // ...
}
```

```html
<bit-table-v2 [table]="table">
  <bit-column sortable defaultSort="asc">
    <bit-header-cell>Id</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.id; let row">{{ row.id }}</bit-cell>
  </bit-column>
  <bit-column sortable>
    <bit-header-cell>Name</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.name; let row">{{ row.name }}</bit-cell>
  </bit-column>
  <bit-column>
    <bit-header-cell>Email</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.email; let row">{{ row.email }}</bit-cell>
  </bit-column>
</bit-table-v2>
```

`table.ref.X` is a typed column reference: autocomplete lists every property of `User`, typos fail
at compile time, and the row type flows through `*bitCellDef` so `let row` is typed as `User` inside
each cell template.

## The model

`TableModel<T, S>` is parallel to nothing else you have to assemble — it _is_ the assembly. `T` is
the row type; `S` declares synthetic column names (see [Synthetic columns](#synthetic-columns)). It
exposes:

| Facet              | What it owns                                                       |
| ------------------ | ------------------------------------------------------------------ |
| `table.dataSource` | Row data, sort state, filter application.                          |
| `table.columns`    | Typed refs (`table.ref` is shorthand), display order, visibility.  |
| `table.filter`     | Search term, facet definitions, applied state, composed predicate. |
| `table.selection`  | Selection state — present only when `selection` is configured.     |

Construct it with one config object; every facet is optional:

```ts
const table = new TableModel<User, "actions">({
  data: users,
  columns: { order: ["name", "email"] },
  filter: { search: (row, term) => row.name.toLowerCase().includes(term.toLowerCase()) },
  selection: { multiple: true, canSelect: (user) => user.canManage },
});
```

## Column definitions

Each `<bit-column>` is a thin wrapper carrying column-level metadata. The column key, the cell
template, and the row context come from a `*bitCellDef` child; the header markup comes from a
`<bit-header-cell>` child.

| Input         | Type                | Notes                                                           |
| ------------- | ------------------- | --------------------------------------------------------------- |
| `sortable`    | `boolean`           | Click-to-sort on the header.                                    |
| `defaultSort` | `"asc"` \| `"desc"` | Initial sort direction. First column with this set wins.        |
| `sortFn`      | `SortFn`            | Custom comparator. See [Sorting](#sorting).                     |
| `width`       | `string`            | Grid track size (e.g. `"120px"`, `"1fr"`). Defaults to `"1fr"`. |

### `*bitCellDef` — the cell template

The structural directive `*bitCellDef` captures the per-row cell template and identifies which
column it belongs to. Its required input is a typed column reference from the model (`table.ref.*`).

```html
<bit-column>
  <bit-cell *bitCellDef="table.ref.email; let row">{{ row.email }}</bit-cell>
</bit-column>
```

`let row` is typed as `User` via `ngTemplateContextGuard` — `row.emial` fails to compile.

### `<bit-header-cell>` — the header template

The header markup is a plain `<bit-header-cell>` child of the column. No `*bitCellDef`-style
directive — the column reads the cell's template directly, and the column key already lives on
`*bitCellDef`.

```html
<bit-column sortable>
  <bit-header-cell><i class="bwi bwi-envelope tw-me-1"></i> Email</bit-header-cell>
  <bit-cell *bitCellDef="table.ref.email; let row">{{ row.email }}</bit-cell>
</bit-column>
```

The sort button affordance is added by `<bit-header-cell>` automatically when the surrounding
`<bit-column>` is sortable. Click toggles direction; aria-sort and aria-pressed are wired for you.

### Synthetic columns

For columns that don't correspond to a field on `T` (action columns, expand/collapse, etc.), declare
them via the model's second type parameter, then reference them like any other column:

```ts
const table = new TableModel<User, "actions">({ data: users });
```

```html
<bit-column>
  <bit-header-cell></bit-header-cell>
  <bit-cell *bitCellDef="table.ref.actions; let row">
    <button bitIconButton="bwi-ellipsis-v">Actions</button>
  </bit-cell>
</bit-column>
```

`table.ref.actions` still carries the row type, so `let row` is typed `User`.

### Reordering and hiding columns

`columns.order` sets display order; columns it omits aren't rendered even though they're declared.
Toggle visibility at runtime with `hide` / `show` / `showAll`:

```ts
const table = new TableModel<User>({ data: users, columns: { order: ["name", "id"] } });
// later: table.columns.hide("name"); table.columns.show("name"); table.columns.showAll();
```

```html
<bit-table-v2 [table]="table">
  <bit-column>
    <bit-header-cell>Id</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.id; let row">{{ row.id }}</bit-cell>
  </bit-column>
  <bit-column>
    <bit-header-cell>Name</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.name; let row">{{ row.name }}</bit-cell>
  </bit-column>
  <bit-column>
    <bit-header-cell>Email (not displayed)</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.email; let row">{{ row.email }}</bit-cell>
  </bit-column>
</bit-table-v2>
```

### Composing columns into reusable wrappers

Columns register via DI lookup of the ancestor `<bit-table-v2>`, so a `<bit-column>` inside another
component's view still finds the table. Pass the model in for its typed refs:

```ts
@Component({
  selector: "demo-status-column",
  imports: [BitColumnComponent, BitCellDefDirective, BitHeaderCellComponent, BitCellComponent],
  template: `
    <bit-column sortable>
      <bit-header-cell>Status</bit-header-cell>
      <bit-cell *bitCellDef="table().ref.status; let row">
        <span class="…">{{ row.status }}</span>
      </bit-cell>
    </bit-column>
  `,
})
class DemoStatusColumnComponent {
  readonly table = input.required<TableModel<User>>();
}
```

Used as:

```html
<bit-table-v2 [table]="table">
  <bit-column>
    <bit-header-cell>Name</bit-header-cell>
    <bit-cell *bitCellDef="table.ref.name; let row">{{ row.name }}</bit-cell>
  </bit-column>
  <demo-status-column [table]="table" />
</bit-table-v2>
```

## Cell content layout

`<bit-cell>` provides a slot vocabulary for rich cells — a leading icon or tile (`slot=start`), the
title (default slot), a subtitle (`slot=secondary`), and a trailing affordance (`slot=end`). Plain
cells use only the default slot and the layout collapses to bare content.

```html
<bit-column sortable defaultSort="asc">
  <bit-header-cell>Name</bit-header-cell>
  <bit-cell *bitCellDef="table.ref.name; let row">
    <bit-icon-tile slot="start" icon="bwi-globe" size="sm" />
    {{ row.name }}
    <span slot="secondary">{{ row.email }}</span>
  </bit-cell>
</bit-column>
```

The `truncate` input (default `true`) controls whether title and subtitle truncate with an ellipsis
or wrap. Slot=secondary and slot=end are hidden when empty, so plain cells render exactly the
default slot's content with no empty wrappers.

## Manual mode

For simple presentational tables — no sort, filter, selection, or virtualization — skip the model
and `<bit-column>`, and project rows directly:

```html
<bit-table-v2>
  <bit-header-row>
    <bit-header-cell>Id</bit-header-cell>
    <bit-header-cell>Name</bit-header-cell>
  </bit-header-row>
  @for (r of items(); track r.id) {
  <bit-row>
    <bit-cell>{{ r.id }}</bit-cell>
    <bit-cell>{{ r.name }}</bit-cell>
  </bit-row>
  }
</bit-table-v2>
```

The consumer projects one `<bit-header-row>` and any number of `<bit-row>` components. The header
row picks up the design's styling automatically. Manual mode has no `[table]` — the consumer
iterates their own data directly; columns default to equal-share (`grid-auto-columns: 1fr`).

Mode selection is automatic: any projected `<bit-column>` ⇒ column-def mode; otherwise the
consumer's `<bit-header-row>` and `<bit-row>` components render directly. Don't mix the two within a
single table — if you need any of the framework features (sort, selection, etc.), use column-def.

## Sorting

`sortable` on `<bit-column>` enables click-to-sort. The cycle is unsorted → ascending → descending →
ascending. Apply an initial sort with `defaultSort`:

```html
<bit-column sortable defaultSort="asc">
  <bit-header-cell>Name</bit-header-cell>
  <bit-cell *bitCellDef="table.ref.name; let row">{{ row.name }}</bit-cell>
</bit-column>
```

For non-trivial comparisons, pass a `sortFn`:

```ts
sortByName = (a: User, b: User, direction?: SortDirection) => {
  const result = a.name.localeCompare(b.name);
  return direction === "asc" ? result : -result;
};
```

```html
<bit-column sortable [sortFn]="sortByName">
  <bit-header-cell>Name</bit-header-cell>
  <bit-cell *bitCellDef="table.ref.name; let row">{{ row.name }}</bit-cell>
</bit-column>
```

Sort state lives on the model's data source — see
[Table → Sorting](?path=/docs/component-library-table--docs#sorting) for the mechanics.

## Filtering

Configure filtering on the model's `filter`: a `search` matcher and/or facet definitions. The model
composes them into the predicate the table applies (re-filtering reactively — no manual
`dataSource.filter` assignment):

```ts
const table = new TableModel<Member>({
  data: members,
  filter: {
    search: (row, term) => row.name.toLowerCase().includes(term.toLowerCase()),
    filters: [
      { id: "role:admin", label: "Admin", predicate: (r) => r.role === "admin" },
      { id: "role:user", label: "User", predicate: (r) => r.role === "user" },
    ],
  },
});
```

Apply facets with `table.filter.apply(id)` (also `remove`, `toggle`, `clear`); the search box writes
the `searchTerm` signal for you. Applied facets are surfaced as value-only `AppliedFilter`
(`{ id, label }`) for the toolbar — the predicate logic stays in the definitions, keyed off `id`.
Omit a facet's `predicate` for server-side filtering and read `table.filter.appliedFilters()` to
build your query. The toolbar is the UI for this state — see [Toolbar](#toolbar).

## Toolbar

`<bit-table-toolbar>`, projected into the table, renders inside the chrome above the header row and
owns the applied-filters row (clear-all, count, dismissible chips). A `<bit-search>` goes in its own
slot (rendered first, on the left) and binds to the model's `filter` automatically — the table
provides it as a `SEARCH_CONSUMER`, so the search needs no `ngModel` or output binding. Other left
controls use `slot="start"`; actions use `slot="end"`:

```html
<bit-table-v2 [table]="table">
  <bit-table-toolbar>
    <bit-search class="tw-flex-1" placeholder="Search"></bit-search>
    <button slot="start" bitButton buttonType="secondary" [bitMenuTriggerFor]="filterMenu">
      Filters
    </button>
    <button slot="end" bit-chip-action label="Add" startIcon="bwi-plus"></button>
  </bit-table-toolbar>
  <bit-column>...</bit-column>
</bit-table-v2>

<bit-menu #filterMenu>
  <button bitMenuItem (click)="table.filter.apply('role:admin')">Admin</button>
  <button bitMenuItem (click)="table.filter.apply('role:user')">User</button>
</bit-menu>
```

The Filters menu calls `table.filter.apply(id)`; the toolbar renders each applied facet as a
dismissible chip and handles removal and clear-all. Any component implementing `SearchConsumer` (not
just the table) gets the same automatic `<bit-search>` wiring.

## Selection

Configure `selection` on the model and the table prepends an internal checkbox column (generated by
the table; consumers don't declare it):

```ts
const table = new TableModel<User>({ data: users, selection: { multiple: true } });
```

```html
<bit-table-v2 [table]="table">…columns…</bit-table-v2>
```

The header checkbox targets the filtered rows (matches CDK conventions). Selected rows pick up a
soft background. Read or mutate the selection via `table.selection`.

### Selectable rows

When only some rows are selectable, supply a `canSelect` predicate. The table renders a checkbox
only for selectable rows, and select-all / indeterminate scope to them; the predicate is also
enforced when selecting, so a non-selectable row can't be selected even programmatically.

```ts
const table = new TableModel<User>({
  data: users,
  selection: { multiple: true, canSelect: (user) => user.canManage },
});
```

### Bulk actions

`<bit-bulk-actions-bar>` projected inside the table reads `selectedCount` implicitly from the
model's selection via DI; its clear button also clears the selection. No explicit wiring:

```html
<bit-table-v2 [table]="table">
  <bit-bulk-actions-bar>
    <bit-bulk-action [action]="onDelete" icon="bwi-trash" label="Delete" />
    <bit-bulk-action [action]="onArchive" icon="bwi-archive" label="Archive" />
  </bit-bulk-actions-bar>

  …columns…
</bit-table-v2>
```

Outside a table, the bar's explicit API (`[selectedCount]`, `(clear)`) still works — see
[Bulk Actions Bar](?path=/docs/component-library-bulk-actions-bar--docs).

## Virtual scrolling

For large datasets, set `[rowSize]` (the height per row, in px). The table renders inside a
`cdk-virtual-scroll-viewport`; only the visible rows are stamped:

```html
<bit-table-v2 [table]="table" [rowSize]="64" [trackBy]="trackById">…columns…</bit-table-v2>
```

`[rowSize]` forces `table-fixed` layout regardless of `[layout]` — virtual scrolling needs
predictable row geometry. Specify `width="…"` on columns for stable widths.

`[trackBy]` is optional but recommended for stable identity across data updates.

## Type safety

`BitTableV2Component<T>` is generic over the row type, inferred from `[table]`. The
`TableModel<T>`'s `table.ref.X` references (fields and synthetics alike) carry `T` to `*bitCellDef`,
which uses `ngTemplateContextGuard` to type `let row` as `T`. Result:

- `table.ref.email.typed` fails (no such property)
- `table.ref.emial` fails (typo)
- `let row` inside `*bitCellDef` is typed `T` — `row.email` autocompletes, `row.emial` fails

This is meaningfully stronger than `MatTable`, which provides only static-API typing (cell `let-row`
is `any`).

## Accessibility

- Always include a header — even an empty one — for each column; screen readers use it to
  contextualize cell data.
- Avoid spanning data across cells.
- For repeating row-level actions, give each one an associating label — `aria-label="Edit Netflix"`
  rather than a bare "Edit".
- The select-all checkbox uses `aria-label="Select all rows"`; row checkboxes use
  `aria-label="Select row"`. Override via the cell template if richer context is needed.
- Sort buttons expose `aria-sort` (`"ascending"` / `"descending"`) on the `<th>` and `aria-pressed`
  on the button.
