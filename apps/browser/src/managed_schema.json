{
  "type": "object",
  "properties": {
    "environment": {
      "type": "object",
      "properties": {
        "base": {
          "type": "string",
          "description": "Base url, typically only this value needs to be set"
        },
        "webVault": { "type": "string" },
        "api": { "type": "string" },
        "identity": { "type": "string" },
        "icons": { "type": "string" },
        "notifications": { "type": "string" },
        "events": { "type": "string" }
      }
    },
    "skipWelcomeOnInstall": {
      "type": "boolean",
      "description": "When true, suppresses the welcome tab that opens on first install."
    }
  }
}
