import { bitPolicyEditRegister } from "./policy-edit-register";

describe("bitPolicyEditRegister", () => {
  it("is not empty (sanity check)", () => {
    expect(bitPolicyEditRegister.length).toBeGreaterThan(0);
  });
});
